{
  "name": "marvel-character-explorer",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm ci --prefix server --include=dev && npm ci --prefix client --include=dev && npm run build --prefix server && npm run build --prefix client",
    "start": "npm start --prefix server",
    "test": "npm run build --prefix client && npm run lint --prefix client && npm run typecheck --prefix client && npm run typecheck --prefix server && npm test --prefix server"
  },
  "engines": {
    "node": ">=24 <25"
  }
}